export enum LanguageEnum {
  En = 'en',
  Fr = 'fr'
}

export const LanguageLocales: { [k: string]: string } = {
  'en': 'en-US',
  'fr': 'fr-FR'
}

export enum FormatDates{
  ddMMyyyy = 'dd/MM/yyyy',
  MMddyyyy = 'MM/dd/yyyy',
}